# ABS conda 环境配置
# 使用: conda env create -f /home/robot/abs/env/environment.yml
name: abs
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - torch==2.0.1
    - torchvision==0.15.2
    - numpy==1.21
    - tensorboard
    - setuptools==59.5.0
